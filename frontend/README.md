# frontend — CosmoChem UI (React + Tailwind)

Phase A는 `../prototype/index.html`(자립형 HTML)에 동작 중.
Phase B에서 Vite + React로 이관하고 backend API와 연동한다.

```bash
# 이관 시
npm create vite@latest . -- --template react
npm install && npm run dev
```
